<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/cardItemRoot"
    android:layout_width="match_parent"
    android:layout_height="98dp"
    android:layout_margin="@dimen/min8dp">

    <TextView
        android:id="@+id/cardItemModel"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:layout_toStartOf="@+id/cardItemPrice"
        android:gravity="center"
        android:text="@string/app_name"
        android:textColor="@color/black"
        android:textSize="@dimen/textSize20sp" />

    <TextView
        android:id="@+id/cardItemPrice"
        android:layout_width="@dimen/size100dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="@+id/cardItemModel"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:gravity="end"
        android:text="@string/app_name"
        android:textColor="@color/purple_700"
        android:textSize="@dimen/textSize20sp" />

</RelativeLayout>